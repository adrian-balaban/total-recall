node_modules/
coverage/
*.tsbuildinfo
**/.remember

# Stryker mutation testing scratch dir (never commit)
.stryker-tmp/

# Build output — produced by CI (release workflow) or by `npm run build`
# locally / from install.sh. Never committed; see CLAUDE.md "Build artifacts".
dist/
