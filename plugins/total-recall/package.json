{
  "name": "total-recall",
  "version": "1.1.10",
  "description": "Persistent memory MCP server for Claude Code and Gemini CLI",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "sync:version": "node scripts/sync-version.mjs",
    "build:esbuild": "./node_modules/.bin/esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist/index.js --define:__PLUGIN_VERSION__=\"\\\"$npm_package_version\\\"\" --external:fsevents --external:@huggingface/transformers --external:sqlite-vec --external:better-sqlite3",
    "build:frontmatter": "./node_modules/.bin/esbuild src/frontmatter.ts --bundle --platform=node --format=esm --outfile=dist/frontmatter.mjs",
    "build:privacy-filter": "./node_modules/.bin/esbuild src/privacy-filter.ts --bundle --platform=node --format=esm --outfile=dist/privacy-filter.mjs",
    "build": "npm run sync:version && npm run build:esbuild && npm run build:frontmatter && npm run build:privacy-filter",
    "build:watch": "npm run build:esbuild -- --watch",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "test:integration": "npm run build && vitest run --config vitest.integration.config.ts",
    "mutation": "stryker run",
    "release:build": "npm run typecheck && npm test && npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^9.6.1",
    "@stryker-mutator/typescript-checker": "^9.6.1",
    "@stryker-mutator/vitest-runner": "^9.6.1",
    "@vitest/coverage-v8": "^4.1.8",
    "esbuild": "^0.28.1",
    "fast-check": "^4.9.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^4.1.8"
  },
  "optionalDependencies": {
    "@huggingface/transformers": "^3.8.1",
    "better-sqlite3": "^12.10.0",
    "sqlite-vec": "^0.1.9"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
